# Server only. Set a Gemini model available to your API account.
GEMINI_API_KEY=
GEMINI_MODEL=
UNSPLASH_ACCESS_KEY=

# Public URL of your own TTS service (POST JSON: text, language; returns audio).
# Use HTTPS for production and allow the website origin in the service's CORS settings.
NEXT_PUBLIC_TTS_URL=
